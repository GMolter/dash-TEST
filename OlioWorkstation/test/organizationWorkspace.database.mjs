import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { pathToFileURL } from 'node:url';
import assert from 'node:assert/strict';
const { PGlite } = await import(pathToFileURL(resolve(process.argv[2] || '.org-sql-test.local/package/dist/index.js')).href);
const db = new PGlite();
const ids = Array.from({length: 6}, (_, i) => `00000000-0000-4000-8000-00000000000${i+1}`);
const [owner, admin, member, outsider, coowner, banned] = ids;
const team='10000000-0000-4000-8000-000000000001';const other='10000000-0000-4000-8000-000000000002';
let checks=0;
async function as(id) { await db.exec('reset role'); await db.query("select set_config('request.jwt.claim.sub',$1,false)",[id]); await db.exec('set role authenticated'); }
async function fails(sql,args=[],message) { await assert.rejects(db.query(sql,args), e => !message || e.message.includes(message)); checks++; }
async function role(id,newRole){await db.query("select public.manage_organization_member($1,$2,'role',$3)",[team,id,newRole]);}
try {
 await db.exec(`create role anon; create role authenticated; create role service_role bypassrls; create schema auth;
 create function auth.uid() returns uuid language sql stable as $$ select nullif(current_setting('request.jwt.claim.sub',true),'')::uuid $$;
 create table auth.users(id uuid primary key, banned_until timestamptz);
 create function public.current_account_is_allowed() returns boolean language sql stable security definer as $$ select exists(select 1 from auth.users where id=auth.uid() and (banned_until is null or banned_until < now())) $$;
 create table organizations(id uuid primary key default gen_random_uuid(),name text,code text,owner_id uuid references auth.users);
 create table profiles(id uuid primary key references auth.users on delete cascade,org_id uuid references organizations on delete set null,role text default 'member',display_name text,email text);
 create table quicklinks(id uuid primary key default gen_random_uuid(),org_id uuid references organizations on delete cascade,title text,url text,scope text);
 create table projects(id uuid primary key default gen_random_uuid(),org_id uuid references organizations on delete cascade,name text,status text);
 alter table organizations enable row level security; alter table profiles enable row level security;
 grant usage on schema public,auth to authenticated,service_role;
 grant all on organizations,profiles,quicklinks,projects to authenticated,service_role;
 create policy old_org_access on organizations for all to authenticated using(true) with check(true);
 create policy old_profile_access on profiles for all to authenticated using(true) with check(true);
 insert into auth.users(id) values ${ids.map(id=>`('${id}')`).join(',')};
 insert into organizations values ('${team}','Team','1234','${owner}'),('${other}','Other','5678','${outsider}');
 insert into profiles(id,org_id,role,display_name) values ('${owner}','${team}','owner','Owner'),('${admin}','${team}','admin','Admin'),('${member}','${team}','member','Member'),('${outsider}','${other}','owner','Other'),('${coowner}','${team}','owner','Coowner'),('${banned}','${team}','member','Banned');
 update auth.users set banned_until=now()+interval '1 day' where id='${banned}';`);
 await db.exec(readFileSync('supabase/migrations/20260921200000_organization_workspace.sql','utf8'));
 await as(owner);
 const a=(await db.query("insert into org_announcements(org_id,title,body) values ($1,'Welcome','Welcome team') returning id",[team])).rows[0].id;checks++;
 await as(member);
 assert.equal((await db.query('select * from org_announcements')).rows.length,1);checks++;
 await fails("insert into org_announcements(org_id,title,body) values ($1,'No','No')",[team]);
 const r=(await db.query("insert into org_resources(org_id,title,kind,content) values ($1,'Guide','note','Team guide') returning id",[team])).rows[0].id;checks++;
 // Reapply with populated tables, then repair a partial installation, preserving every row.
 await db.exec('reset role');
 const snapshot = async () => {
   const data = {};
   for (const table of ['organizations','profiles','org_announcements','org_resources','org_activity']) {
     data[table] = (await db.query('select * from public.' + table + ' order by id')).rows;
   }
   return data;
 };
 const original = await snapshot();
 const migration = readFileSync('supabase/migrations/20260921200000_organization_workspace.sql','utf8');
 await db.exec(migration);
 assert.deepEqual(await snapshot(), original); checks++;
 await db.exec('drop policy organization_workspace_read on public.organizations; drop trigger activity_resources on public.org_resources;');
 await db.exec(migration);
 assert.deepEqual(await snapshot(), original); checks++;
 assert.equal((await db.query("select count(*)::int as total from pg_policies where schemaname='public' and tablename='organizations' and policyname='organization_workspace_read'")).rows[0].total,1); checks++;
 assert.equal((await db.query("select count(*)::int as total from pg_trigger where tgname='activity_resources' and tgrelid='public.org_resources'::regclass")).rows[0].total,1); checks++;
 await as(member);
 await fails("insert into org_resources(org_id,title,kind) values ($1,'Bad link','link')",[team]);
 await fails("insert into org_resources(org_id,title,kind,url) values ($1,'Bad link','link','javascript:alert(1)')",[team]);
 await fails('update org_resources set org_id=$1 where id=$2',[other,r]);
 await fails("update profiles set role='owner' where id=$1",[member]);
 await fails("select manage_organization_member($1,$2,'role','owner')",[team,member]);
 await fails("insert into org_activity(org_id,actor_name,category,action,subject) values ($1,'Fake','people','joined','Fake')",[team]);
 await as(outsider);
 assert.equal((await db.query('select * from org_resources')).rows.length,0);assert.equal((await db.query('select * from org_announcements')).rows.length,0);assert.equal((await db.query('select * from org_activity')).rows.length,0);checks++;
 await fails("select manage_organization_member($1,$2,'role','owner')",[team,member]);
 await as(admin);
 await fails("select manage_organization_member($1,$2,'role','owner')",[team,member]);
 await fails("select manage_organization_member($1,$2,'remove')",[team,owner]);
 await db.query("update org_resources set title='Edited guide' where id=$1",[r]);checks++;
 await role(member,'admin');checks++;
 await as(owner);await role(member,'member');await role(admin,'owner');checks++;
 await db.query("select manage_organization_member($1,$2,'transfer')",[team,member]);checks++;
 assert.equal((await db.query('select role from profiles where id=$1',[owner])).rows[0].role,'admin');
 await as(member);await role(coowner,'member');await role(admin,'admin');
 await fails("select manage_organization_member($1,$2,'role','admin')",[team,member],'last owner');
 await fails("select manage_organization_member($1,$2,'leave')",[team,member],'last owner');
 await as(banned);assert.equal((await db.query('select * from org_announcements')).rows.length,0);checks++;
 await as(member);
 await db.query("insert into quicklinks(org_id,title,url,scope) values ($1,'Private bookmark','https://private.test','personal')",[team]);
 assert.equal((await db.query("select * from org_activity where subject='Private bookmark'")).rows.length,0);checks++;
 await db.query("insert into quicklinks(org_id,title,url,scope) values ($1,'Shared bookmark','https://team.test','shared')",[team]);
 assert.equal((await db.query("select * from org_activity where category='links'")).rows.length,1);checks++;
 await db.query("insert into projects(org_id,name) values ($1,'Team project')",[team]);
 await db.query("insert into projects(org_id,name) values (null,'Personal project')");
 assert.equal((await db.query("select * from org_activity where category='projects'")).rows.length,1);checks++;

 // Application admin management: isolated from authenticated org owners.
 await db.exec('reset role; alter table profiles add column app_admin boolean default false, add column app_owner boolean default false;');
 await db.query('update profiles set app_admin=true where id=$1',[admin]);
 const adminMigration=readFileSync('supabase/migrations/20260922200000_admin_organization_management.sql','utf8');
 await db.exec(adminMigration); await db.exec(adminMigration); checks++;
 await as(member);
 await fails("select admin_manage_organization_member($1,$2,$3,'set-member','owner')",[admin,team,owner],'permission denied');
 await fails('update org_announcements set created_by=$1 where id=$2',[admin,a]);
 await db.exec('reset role; set role service_role;');
 await fails("select admin_manage_organization_member($1,$2,$3,'set-member','owner')",[owner,team,owner],'administrator');
 await db.query('update profiles set app_owner=true where id=$1',[owner]);
 await fails("select admin_manage_organization_member($1,$2,$3,'set-member','owner')",[admin,team,owner],'application owner');
 await db.query('update profiles set app_owner=false where id=$1',[owner]);
 await fails("select admin_manage_organization_member($1,$2,$3,'remove-member')",[admin,team,member],'last owner');
 await fails("select admin_manage_organization_member($1,$2,$3,'set-member','member')",[admin,team,outsider],'current organization');
 await db.query("select admin_manage_organization_member($1,$2,$3,'set-member','owner')",[admin,team,owner]); checks++;
 assert.equal((await db.query("select count(*)::int as n from profiles where org_id=$1 and role='owner'",[team])).rows[0].n,2); checks++;
 await db.query("select admin_manage_organization_member($1,$2,$3,'transfer-owner',null,$4)",[admin,team,coowner,owner]);
 assert.equal((await db.query('select role from profiles where id=$1',[owner])).rows[0].role,'admin'); checks++;
 await db.query("select admin_manage_organization_member($1,$2,$3,'remove-member')",[admin,team,coowner]);
 assert.equal((await db.query('select org_id from profiles where id=$1',[coowner])).rows[0].org_id,null); checks++;
 await db.query("select admin_manage_organization_member($1,$2,$3,'set-member','member')",[admin,team,coowner]); checks++;
 await db.query("update org_announcements set created_by=$1, created_at='2020-01-02T00:00:00Z',updated_at='2020-01-03T00:00:00Z',pinned=true where id=$2",[admin,a]);
 const edited=(await db.query('select * from org_announcements where id=$1',[a])).rows[0];
 assert.equal(edited.created_by,admin);assert.equal(edited.created_at.toISOString(),'2020-01-02T00:00:00.000Z');assert.equal(edited.updated_at.toISOString(),'2020-01-03T00:00:00.000Z');checks++;
 await db.query('update org_resources set org_id=$1,created_by=$2 where id=$3',[other,outsider,r]); checks++;
 await db.query('update org_resources set org_id=$1 where id=$2',[team,r]);
 const event=(await db.query("insert into org_activity(org_id,actor_name,category,action,subject) values ($1,'Admin','settings','corrected','History') returning id",[team])).rows[0].id;
 await db.query("update org_activity set subject='Corrected',created_at='2020-01-01' where id=$1",[event]);
 assert.equal((await db.query('delete from org_activity where id=$1 returning id',[event])).rows.length,1);checks++;
 await as(owner); assert.equal((await db.query('delete from organizations where id=$1 returning id',[team])).rows.length,0);checks++;
 await as(member); assert.equal((await db.query('delete from organizations where id=$1 returning id',[team])).rows.length,1);checks++;
 assert.equal((await db.query('select * from org_resources')).rows.length,0);checks++;
 console.log(`PASS: ${checks} organization database checks (reruns, data preservation, partial repair, content, permissions, ownership, privacy, deletion).`);
} finally {await db.close();}
